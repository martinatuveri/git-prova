function Logo() {
  const nome = 'Martina';
  const cognome = 'Tuveri';

  return (
    <div className="header">
      <h1>{nome} {cognome}</h1>
    </div>
  );
}

// feature pull
export default Logo;
